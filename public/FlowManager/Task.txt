Updates:
    FlowManager:
        - save Flow Manager instructions to the database when clicked the "Save Flow" button
        - save the diagrame code which prints in concole when clicked the "Save Flow" button
        - next time the user enters the Flow Manager, the last saved diagram should be loaded
        - the compiled code also should be saved in the database.

    manageAccount.html:
        - add a dropdown menu to select in which method the bot should reply (Menu Reply, Auto Reply, Flow Reply, Direct AI)
        
    - when the Flow Reply is selected the bot should reply to whatsapp as it worked in the simulator
    