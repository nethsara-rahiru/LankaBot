Found some issues in the system

1. currently in the system if user deletes its session, all the data with is also deletes (Rules, Flows, Menus and etc..)

    Fix:
    - Add a option to rescan the QR code and restore the session in the accounts.html
    
2. need a method to create sessions for remote customers. usualy a admin manages the system. so, if the client is not near by that can't scan the QR. need a solution for this.

3. audienceInsights.html is not working.

    Fix:
    - Fully reupdate the page.
    - datas are not properly frtching sence the database schema was changed.
    - Add a button to restart the user's conversation.
    - load a chatbox to show all the chats the user had done with the session.
    - show all knowen data of it (Variables got in flow messaging)
    - remove ai chat summary, and the button

