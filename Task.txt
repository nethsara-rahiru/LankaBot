Create a new page in public named "userManagment.html", to manage users in the system.
in that page:
    - Admins should be able to which sessions are handeled by who
    - assign roles to users (user, admin, manageer, analyzer, etc)
    - assign sessions to users to handel them
    - admins should be able to delete users.

Frontend:
    - all the users should be in a table
    - columns should be:
        1. name (show role if it is not a normal user)
        2. status (loged in, loged out, blocked, online)
        3. number of sessions handeled
        4. action (edit, delete, block)
    - when a row is clicked expan the row and show the sessions handeled by that user as a list
    - add a button to the each row of the list to remove from him.
    - add a button to add session to let him handel. the button should open a popup to search session by number and add it.
    - A search bar should be there in the top right